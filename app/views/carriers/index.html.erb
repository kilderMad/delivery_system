<section>
  <h1>Transportadoras</h1>
  
  <div>
    <%= link_to 'Cadastrar', new_carrier_path%>
  </div>
  <% if @carriers.empty?%>
    <div>
      <p>Não há transportadoras cadastradas</p>
    </div>
  <% else %>
    <table class='carriers'>
    <thead>
      <tr>
        <th>Nome</th>
        <th>CNPJ</th>
        <th>Domínio</th>
        <th>Endereço</th>
        <th>Email</th>
      </tr>
    </thead>
    <tbody>
    <% @carriers.each do |c|%>
        <tr>
          <td><%= link_to c.fantasy_name, c %></td>
          <td><%= c.cnpj %></td>
          <td><%= c.domain %></td>
          <td><%= c.address %></td>
          <td><%= c.email %></td>
        </tr>
    <%end%>
    </tbody>
  </table>
  <%end%>
</section>